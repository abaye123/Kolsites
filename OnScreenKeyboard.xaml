<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Kolsites.OnScreenKeyboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kolsites">

    <UserControl.Resources>
        <Style x:Key="KeyButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="58"/>
            <Setter Property="MinHeight" Value="58"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style x:Key="WideKeyButtonStyle" TargetType="Button" BasedOn="{StaticResource KeyButtonStyle}">
            <Setter Property="MinWidth" Value="120"/>
        </Style>
        <Style x:Key="LayoutToggleStyle" TargetType="Button" BasedOn="{StaticResource KeyButtonStyle}">
            <Setter Property="MinWidth" Value="78"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="0,1,0,0"
          Padding="6">
        <StackPanel x:Name="KeyboardRoot" Spacing="0" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
